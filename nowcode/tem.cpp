#include <algorithm>
#include <iostream>
#include <vector>
#include <queue>
#include <stdint.h>
#include <string>
#include <climits>

#define endl "\n"

using namespace std;
using ll = long long;
using vll = vector<ll>;
using vint = vector<int>;

void _() {
    string s;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t = 1;
    // cin >> t;
    while (t--) _();

    cout.flush();

    system("pause");
}