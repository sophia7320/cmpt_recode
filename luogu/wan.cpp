#include <algorithm>
#include <iostream>
#include <vector>
#include <queue>
#include <stdint.h>
#include <string>
#include <climits>

#include <windows.h>

//#define endl "\n"

using namespace std;
using ll = long long;
using vll = vector<ll>;
using vint = vector<int>;

void _() {
    char c = '吃';
    cout << c << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    SetConsoleCP(CP_UTF8);
    SetConsoleOutputCP(CP_UTF8);

    int t = 1;
    // cin >> t;
    while (t--) _();

    cout.flush();

    system("pause");
}